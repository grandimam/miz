[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "miz"
version = "0.1.0"
description = "Build expertise. Validate it. Own it. A stack of AI-agent skills for learning and career."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "Gaurav Mukherjee" }]
keywords = ["learning", "career", "interview-prep", "skills", "ai-agents", "spaced-repetition"]
classifiers = [
  "Programming Language :: Python :: 3",
  "License :: OSI Approved :: MIT License",
  "Operating System :: OS Independent",
]

[project.urls]
Homepage = "https://github.com/grandimam/miz"
Repository = "https://github.com/grandimam/miz"

[project.scripts]
miz = "miz.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/miz"]

[tool.hatch.build.targets.wheel.force-include]
home = "miz/skills/home"
career = "miz/skills/career"
curriculum = "miz/skills/curriculum"
learn = "miz/skills/learn"
probe = "miz/skills/probe"
book = "miz/skills/book"
resume = "miz/skills/resume"

[tool.hatch.build.targets.sdist]
include = [
  "src",
  "home",
  "career",
  "curriculum",
  "learn",
  "probe",
  "book",
  "resume",
  "test",
]